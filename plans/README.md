# plans/

Histórico de planos de execução do GarraIA. Cada plano está atrelado a uma issue GAR-* no Linear e é aprovado antes da execução.

## Convenção de nome

`NNNN-gar-XXX-slug-descritivo.md`

- `NNNN` — sequencial monotônico (`0001`, `0002`, ...) — ordem cronológica de criação.
- `gar-XXX` — issue Linear principal que o plano entrega.
- `slug-descritivo` — identificador humano curto em kebab-case.

## Regras

- **Aprovação obrigatória:** nenhum plano vira código sem "Plano aprovado" explícito do owner.
- **Imutável após merge:** um plano é o registro histórico de como a decisão foi tomada. Se o escopo mudar, crie um novo plano (`NNNN+1`) que o supersede.
- **Escopo claro:** `§1 Goal`, `§3 Scope/Non-scope`, `§4 Acceptance criteria` são obrigatórios.
- **Rollback plan:** todo plano precisa de `§8 Rollback plan` — se é reversível, como; se não é, por quê.
- **Open questions:** dúvidas que bloqueiam execução ficam no `§12 Open questions` e precisam ser respondidas antes do start.

## Index

| # | Plano | Issue | Status |
|---|---|---|---|
| 0001 | [OpenTelemetry + Prometheus baseline](0001-gar-384-opentelemetry-baseline.md) | [GAR-384](https://linear.app/chatgpt25/issue/GAR-384) | ✅ Merged 2026-04-13 (`84c4753`) |
| 0002 | [ADR 0003 — Database para Group Workspace](0002-gar-373-adr-postgres-decision.md) | [GAR-373](https://linear.app/chatgpt25/issue/GAR-373) | ✅ Merged 2026-04-13 (`32dba08`) |
| 0003 | [`garraia-workspace` crate + migration 001 (users & groups)](0003-gar-407-workspace-schema-bootstrap.md) | [GAR-407](https://linear.app/chatgpt25/issue/GAR-407) | ✅ Merged 2026-04-13 (`4c0f07e`) |
| 0004 | [Migration 002 — RBAC + audit_events](0004-gar-386-migration-002-rbac.md) | [GAR-386](https://linear.app/chatgpt25/issue/GAR-386) | ✅ Merged 2026-04-13 (`54cefca`, closes GAR-414) |
| 0005 | [Migration 004 — chats + messages + FTS](0005-gar-388-migration-004-chats-fts.md) | [GAR-388](https://linear.app/chatgpt25/issue/GAR-388) | ✅ Merged 2026-04-13 (`1514227`) |
| 0006 | [Migration 005 — memory_items + pgvector HNSW](0006-gar-389-migration-005-memory-pgvector.md) | [GAR-389](https://linear.app/chatgpt25/issue/GAR-389) | ✅ Merged 2026-04-13 (`d790b9a`) |
| 0007 | [Migration 007 — Row-Level Security (FORCE RLS)](0007-gar-408-migration-007-rls.md) | [GAR-408](https://linear.app/chatgpt25/issue/GAR-408) | ✅ Merged 2026-04-13 (`18d0326`) |
| 0008 | [Migration 006 — Tasks (Tier 1) com RLS embutido](0008-gar-390-migration-006-tasks-with-rls.md) | [GAR-390](https://linear.app/chatgpt25/issue/GAR-390) | ✅ Merged 2026-04-13 (`883399e`) |
| 0009 | [ADR 0005 — Identity Provider (BYPASSRLS + Argon2id + HS256)](0009-gar-375-adr-0005-identity-provider.md) | [GAR-375](https://linear.app/chatgpt25/issue/GAR-375) | ✅ Merged 2026-04-13 (`a89c783`) |
| 0010 | [`garraia-auth` crate skeleton + migration 008 (login role)](0010-gar-391a-garraia-auth-crate-skeleton.md) | [GAR-391a](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-13 (`c5d6350`) |
| 0011 | [`verify_credential` real impl + audit + JWT issuance](0011-gar-391b-verify-credential-impl.md) | [GAR-391b](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-13 (`354d72a`) |
| 0011.5 | [Migration 009 — `user_identities.hash_upgraded_at` (corretiva 391b)](0011.5-gar-391b-migration-009-hash-upgraded-at.md) | [GAR-391b](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-13 (`356e5e4`) |
| 0012 | [Axum extractor + `RequirePermission` + refresh/logout/signup endpoints](0012-gar-391c-extractor-and-wiring.md) | [GAR-391c](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-13 (`88f323e`) |
| 0013 | [RLS matrix (GAR-392) — path C, 391d deferido](0013-gar-391d-392-authz-suite.md) | [GAR-392](https://linear.app/chatgpt25/issue/GAR-392) | ✅ Merged 2026-04-14 (`4069ace` + `1267987`). GAR-391d re-escopado para plan 0014. |
| 0014 | [GAR-391d — App-layer cross-group authz matrix via HTTP](0014-gar-391d-app-layer-authz-matrix.md) | [GAR-391d](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-15 (`a688497`, PR #17). 15-case table-driven matrix em `authz_http_matrix.rs` cobrindo `GET /v1/me`, `POST /v1/groups`, `GET /v1/groups/{id}` × {alice, bob, eve} + novo fixture `seed_user_without_group`. Revisões: `security-auditor` APPROVE (rule 10 satisfeita) + `code-reviewer` APPROVE. **Epic GAR-391 fechado.** |
| 0015 | [Fase 3.4 — REST `/v1` skeleton (slice 1: `GET /v1/me`)](0015-fase-3-4-rest-v1-skeleton.md) | GAR-WS-API (pré-condição GAR-391d) | ✅ Merged 2026-04-14 (`4afb4fe`, PR #8). Entregue **apenas o slice 1**: endpoint `GET /v1/me`, OpenAPI/Swagger em `/docs`, RFC 9457 Problem Details, fail-soft 503 e teste de integração fail-soft. Cobertura autenticada (`200`/`401`/`403` com JWT + Postgres real) foi deferida ao plan 0016. |
| 0016 | [Fase 3.4 — Slice 2: AppPool + harness + authed `/v1/me` + `/v1/groups` skeleton](0016-fase-3-4-slice-2-apppool-harness-groups.md) | GAR-WS-API (GAR-391d destravado após M4) | ✅ **Merged 2026-04-15** — M1 (`3d2fc66`, PR #10) `AppPool` newtype + env var + `AppState.app_pool` + split `RestV1State` + router 3-way match + stubs; M2 (`a6c337d`, PR #12) integration harness (pgvector testcontainer + typed pools + `build_router_for_test`); M3 (`b139861`, PR #13) `SecurityAddon` bearer + `seed_user_with_group` + bundled `v1_me_authed_scenarios`; M4 (`860312b`, PR #15) **real `/v1/groups` handlers** (POST create_group + GET get_group + 7-scenario integration test + `RestError::BadRequest`/`NotFound` variants + `SET LOCAL app.current_user_id` transactional protocol); M5 (`1404365`, PR #20) review follow-ups **corpo formal do plano** (Task 16 H-1/M-3/N-3: `unconfigured_handler → impl IntoResponse`, doc note PII em `RestError::Internal`, `tracing::warn!` no fallback de `problem.rs`; Task 17 M-1: `/docs/{*rest}` catch-all na branch fail-soft). **Destravou definitivamente o plan 0014 (GAR-391d).** **Itens extras do comentário de índice original** (`admin_url` accessor, `exec_with_tenant` closure API, URL parsing, `test-support` rename, `get_group` transactional wrap trigger, cenário 8 coverage gap) **não entraram neste PR por decisão de escopo** — ficam para eventual 0016.1 / 0017 se relevantes. |
| 0017 | [GAR-393 Slice 1 — `PATCH /v1/groups/{group_id}`](0017-gar-393-v1-groups-patch.md) | [GAR-393](https://linear.app/chatgpt25/issue/GAR-393) | ✅ Merged 2026-04-16 (`6077bcc`, PR #23). Slice 1 de GAR-393. Code review APPROVED + security audit 8.5/10 (no blockers). CI 9/9 green. |
| 0018 | [GAR-393 Slice 2 — `POST /v1/groups/{group_id}/invites`](0018-gar-393-v1-groups-invites.md) | [GAR-393](https://linear.app/chatgpt25/issue/GAR-393) | ✅ Merged 2026-04-16 (`7c06cec`, PR #24). Slice 2 de GAR-393. Invite creation com Argon2id token hash, migration 011 UNIQUE index, SQLSTATE 23505 conflict detection. Code review blockers addressed + security audit 8.5/10. CI 9/9 green. |
| 0019 | [GAR-393 Slice 3 — `POST /v1/invites/{token}/accept`](0019-gar-393-v1-invites-accept.md) | [GAR-393](https://linear.app/chatgpt25/issue/GAR-393) | ✅ Merged 2026-04-17 (`d590313`, PR #25). Slice 3 de GAR-393. Accept invite: Argon2id token verify, token length guard (SEC-07), expiração (410), **race-safe UPDATE guard** (`AND accepted_at IS NULL` + `rows_affected == 0` ⇒ 409), member-collision via SQLSTATE 23505 (409), INSERT `group_members` transacional. 6 tasks + review fixes. Path `/accept` em vez de `:accept` por limitação do `matchit` (Axum 0.8). Code review `REQUEST CHANGES` com 1 blocker (B-1 race) → endereçado; security audit 7.0/10 com 9 findings endereçados + 8 deferidos (SEC-01 rate-limit e SEC-04 audit_events para plan 0021). CI 9/9 green. Desbloqueia `setRole` + DELETE member (plan 0020). |
| 0020 | [GAR-393 Slice 4 — setRole + DELETE member](0020-gar-393-v1-members-setrole-delete.md) | [GAR-393](https://linear.app/chatgpt25/issue/GAR-393) | ✅ Merged 2026-04-20 (`664a276`, PR #27). Slice 4 (0020-a do split) de GAR-393 — **fecha o epic 6/6 endpoints**. `POST /v1/groups/{id}/members/{user_id}/setRole` + `DELETE /v1/groups/{id}/members/{user_id}` (soft-delete via `status='removed'`). 8 tasks + 1 review follow-ups commit. Authz em 2 camadas (capability gate com self-bypass + hierarchy gate non-self). Last-owner invariant via `COUNT(*) FILTER (WHERE role='owner' AND status='active')` pós-UPDATE na mesma tx (zero ⇒ 409 + rollback). Promote-to-owner rejeitado em `validate()` (400) + defesa DB via `group_members_single_owner_idx`. `SELECT ... FOR UPDATE` serializa setRole/DELETE concorrentes. Path `/setRole` em vez de `:setRole` por limitação Axum 0.8 / matchit (mesma decisão do plan 0019 `/accept`). Integration tests: 10 M-scenarios + 10 D-scenarios (incl. D2b admin self-leave adicionado no review follow-up) + 8 novos cases na authz matrix (26→34). Code review **APPROVED WITH NITS** (0 blockers). Security audit **8.5/10 APPROVE** (0 blockers; 2 SEC-MEDs endereçados + 1 SEC-LOW addressed). CI 9/9 green. Follow-ups para plan 0021 (0020-b): amend do predicado do partial UNIQUE index (`AND status='active'`) + audit_events + rate-limit dedicado para accept/setRole/DELETE + NITs opcionais (extrair `assert_group_header_matches` e `resolve_caller_role`). |
| 0021 | [GAR-425 — Workspace security hardening (audit + rate-limit + idx)](0021-gar-425-workspace-security-hardening.md) | [GAR-425](https://linear.app/chatgpt25/issue/GAR-425) | ✅ Merged 2026-04-20 (`4753f1d`, PR #30). Slice "0020-b" consolidando 3 follow-ups de segurança deferidos dos reviews de PRs #25 (plan 0019) e #27 (plan 0020). Entregue: (1) **Migration 012** — amendment do predicado do partial UNIQUE `group_members_single_owner_idx` para `WHERE role = 'owner' AND status = 'active'` (alinha DB × app-layer, destrava reativação futura de owners soft-deleted); (2) **Migration 013** — `WITH CHECK` explícito no `audit_events_group_or_self` RLS policy (defesa contra PERMISSIVE→RESTRICTIVE downgrade silencioso); (3) **`audit_events`** para `invite.accepted`/`member.role_changed`/`member.removed` via novo módulo `garraia-auth::audit_workspace` + helper tx-scoped, 1 INSERT por happy-path dentro da mesma tx do handler (atomicidade — rollback descarta audit + mutation juntos); (4) **Rate-limit dedicado** — preset `RateLimitConfig::members_manage()` (20/min, 200/h, burst 5) wired nas 3 rotas privilegiadas (accept, setRole, DELETE). Testes: 2 unit em WorkspaceAuditAction + audit aggregate assertion em v1_groups_scenarios + A1 audit assertion + A7 rate-limit burst. Code review **APPROVE WITH NITS** (0 blockers, 3 MEDIUMs endereçados). Security audit **8.0/10 → APPROVE** (2 HIGH blockers F-01/F-02 endereçados no commit `a3a80b9`). CI 9/9 green (2 infra-flakes do dtolnay/rust-toolchain re-disparados e passaram). Follow-ups deferidos para plan 0022 (workspace-security part 2): decodar JWT `sub` para fix do shared-bucket (F-03), `TRUSTED_PROXIES` env var (F-02 fix completo), DashMap lock refactor (code-review HIGH), rate-limiter NITs (reset_at double-call, count_in_window cast, headers duplicados), `WorkspaceAuditAction::Display` impl, assertion test de `app.current_user_id` set (F-05). |
| 0023 | [GAR-427 — Migrate `api.rs` session-IP stamp to `real_client_ip`](0023-gar-427-xff-api-session-ip.md) | [GAR-427](https://linear.app/chatgpt25/issue/GAR-427) | ✅ Merged 2026-04-20 (`cf0feea`, PR #35). Hybrid PR (plan + README + code + docblock cleanup, 4 arquivos, ~150 linhas). Narrow 1-site slice fechando 1 de 3 `TODO(plan-0023+)` markers deixados pelo plan 0022. `create_session` em `api.rs` agora deriva o IP do session token via `rate_limiter::real_client_ip` com `GARRAIA_TRUSTED_PROXIES` fail-closed (empty env ⇒ XFF ignorado). Handler signature gained `ConnectInfo(peer_addr): ConnectInfo<SocketAddr>` (direct, não `Option<>`) matching `auth_routes::login_handler` — Axum 0.8 não tem `OptionalFromRequestParts for ConnectInfo<T>`. Docblock de `real_client_ip` em `rate_limiter.rs` refreshed para refletir status de consolidação. Code review **APPROVE** (0 blockers, 1 NIT endereçado em `dd760a0`). Security audit **8/10 APPROVE** (0 blockers; 2 HIGH admin out-of-scope → plan 0024, 2 MEDIUM como follow-ups: cache TRUSTED_PROXIES parse + smoke-test ConnectInfo wiring; 2 LOW: IPv6 format docs + parse-warn counter). CI 9/9 green (Windows 8m53s). Follow-ups para plan 0024+: admin XFF consolidation (`admin/middleware.rs::extract_ip` + ~20 `admin/handlers.rs` sites requires `connect_info` propagation + product decision sobre admin audit-trail IP format). |
| 0022 | [GAR-426 — Workspace security hardening part 2 (rate-limit refinements + audit robustness)](0022-gar-426-workspace-security-part-2.md) | [GAR-426](https://linear.app/chatgpt25/issue/GAR-426) | ✅ Merged 2026-04-20 (`7e20884`, PR #33). Fechamento explícito dos 9 follow-ups deferidos dos reviews do PR #30 (plan 0021). Entregue: (1) **Per-user rate-limit key** via JWT `sub` decoding — novo `RateLimitLayerState` + `rate_limit_layer_authenticated` middleware (fix F-03: cada user ganha bucket `jwt-sub:{uuid}`); (2) **`GARRAIA_TRUSTED_PROXIES`** env + helper `real_client_ip` fail-closed (fix completo F-02: empty env ⇒ XFF ignorado); (3) **DashMap lock split** read/write em `RateLimiter::check()` + benign race documentada (CR-HIGH); (4–7) NIT sweep: `reset_at` single-call, `count_in_window` `u32::try_from`, headers deduplicados, **remoção de `X-RateLimit-Reset`** (SEC-LOW F-06 timing leak); (8) **F-05 integration test** `audit_workspace_branch_two.rs` — regression guard para branch 2 da policy `audit_events_group_or_self`; (9) **`WorkspaceAuditAction::Display`** impl (CR-NIT); (10) **A7 burst test deterministic** (20× 404 + 21º=429 exato); + review follow-ups: `#[deprecated]` em `extract_rate_limit_key`/`rate_limit_layer` legacy, 5 unit tests para fallback ladder do `extract_authenticated_rate_limit_key` (gated em `test-helpers`), `TODO(plan-0023+)` inline em `api.rs:71` + `admin/middleware.rs:168`. 9 commits T1-T8 + 1 follow-ups + 1 fix de CI (`required-features = ["test-helpers"]` gate no `[[test]]` block do novo binary). Double-verify trade-off (middleware + extractor ambos chamam `verify_access`, ~µs cost) aceito em v1. Code review **APPROVE WITH NITS** (0 blockers, 5 NITs endereçados). Security audit **8.5/10 APPROVE** (0 blockers, 2 SEC-LOWs endereçados). CI 9/9 green (Windows tomou 27m43s devido a Post Cache upload, mas sem flake). Follow-ups para plan 0023+: consolidar XFF em `api.rs`/`admin/middleware.rs` via shared `real_client_ip`, migrar `/auth/*` do deprecated middleware, cache de claims em `request.extensions()` se p95 degradar, drop das APIs deprecated quando migração completar. |
| 0024 | [GAR-412 — /metrics endpoint auth (Bearer + IP ACL + startup fail-closed)](0024-gar-412-metrics-endpoint-auth.md) | [GAR-412](https://linear.app/chatgpt25/issue/GAR-412) | ✅ Merged 2026-04-21 (`fe1f2db`, PR #38). Security hardening do subsistema de métricas Prometheus cobrindo as duas superfícies `/metrics` antes públicas: (1) listener dedicado spawnado pelo gateway em `GARRAIA_METRICS_BIND` (fail-closed **startup** quando bind não-loopback + auth ausente → `Err(MetricsExporterError::AuthNotConfigured)`, gateway principal fail-soft), (2) rota embedded `GET /metrics` no main listener (fail-closed **runtime** via middleware 401/403/503). Entrega: novo módulo `crates/garraia-gateway/src/metrics_auth.rs` com `MetricsAuthConfig` (`Arc<str>` token + `Arc<[IpNet]>` allowlist cheap-clone) + `metrics_auth_layer` Axum middleware; comparação **hash SHA-256 + `subtle::ConstantTimeEq`** (não `ct_eq` direto — elimina length-oracle, security audit M-1); `WWW-Authenticate: Bearer realm="metrics"` em toda 401 (RFC 7235 §4.1, M-2); CIDR reusa `rate_limiter::parse_trusted_proxies` (plan 0022); peer-unavailable → 503 explícito (code review MEDIUM). Novo `metrics_exporter.rs` com `spawn_dedicated_metrics_listener`. `garraia-telemetry::init_metrics` downgradeado para recorder-only (sem `with_http_listener`); `Guard::metrics_handle()` expõe clone; CLI `init_telemetry_guard → (Option<Guard>, TelemetryConfig)` flow-through uma única snapshot para o gateway (elimina dupla `from_env()`). Env vars novos: `GARRAIA_METRICS_TOKEN` (redigido em `TelemetryConfig::Debug`, listado em CLAUDE.md regra #6 ao lado de `GARRAIA_JWT_SECRET`) + `GARRAIA_METRICS_ALLOW` (comma-separated CIDR). Testes **231/231 passing** (21 unit em `metrics_auth` + 2 em `metrics_exporter` + 10 telemetry + 6 integration end-to-end com `TcpListener` real + reqwest). Code review **REQUEST CHANGES → APPROVE** (1 HIGH doc inconsistency + 2 MEDIUM + 3 LOW — todos endereçados no commit `17016aa`). Security audit **8.5/10 APPROVE** (2 MEDIUMs M-1/M-2 endereçados no mesmo commit, raising para ≥9.0). CI 9/9 green (Windows test incluso). Docs: `docs/telemetry.md` §6.1 Security (matriz 200/401/403/503 + deploy recommendations loopback/VPN/internet/escape-hatch) + §8 troubleshooting (4 novos cenários) + `.env.example` documenta as 2 vars. Breaking change para deploys com `GARRAIA_METRICS_BIND=0.0.0.0`: listener dedicado recusa bind, rota embedded 503 runtime — escape hatch `GARRAIA_METRICS_ALLOW=0.0.0.0/0` (opt-in explícito documentado). Follow-ups deferidos: plan 0025+ admin XFF consolidation (~25 sites em `admin/`, bloqueado por propagação de `ConnectInfo` + decisão de produto sobre admin audit-trail IP format), plan 0026+ `/auth/*` migration off deprecated rate-limit layer (bloqueado por design — register/login mintam o token), GAR-411 TLS em `/metrics` + cardinality guards. |
| 0025 | [GAR-411 — Telemetry hardening (REDACT_HEADERS + idempotent init + TLS docs + cardinality guards)](0025-gar-411-telemetry-hardening.md) | [GAR-411](https://linear.app/chatgpt25/issue/GAR-411) | ✅ Merged 2026-04-21 (`584c047`, PR #39). CI 9/9 green. Narrow slice fechando 4 dos 6 follow-ups de GAR-384: **M5** REDACT_HEADERS +3 entries (`x-forwarded-authorization` + `x-original-authorization` + `x-amzn-trace-id`) para reverse-proxy variants; **L3** `init()` verdadeiramente idempotente (swap `AtomicBool` → `OnceLock<()>`, segunda chamada retorna empty guard sem re-instalar tracer/recorder — regression test strong RED confirmed `attempted to set a recorder after the metrics system was already initialized` antes do fix); **M2** `docs/telemetry.md` §1.1 "TLS for OTLP exports" avisando sobre `tonic` plaintext default + `https://` endpoint recommendation + service-mesh sidecar edge-case; **M1** `debug_assert_route_template()` helper em `metrics.rs` dispara em debug build quando detecta UUID-looking segment (canonical 36-char form) ou long numeric segment (≥6 chars) no route label, `#[inline(always)]` no-op em release — zero runtime cost. **Non-scope (plan 0026+):** M3 `init()` signature change (Result→Guard, API break com 2 callers), L5 nightly cargo-audit CI workflow. Testes: 13 unit + 3 integration (idempotent_init) + 2 smoke = 18/18 green. Zero nova dependência, zero breaking API change. |
| 0026 | [GAR-411 part 2 — init signature + cargo-audit CI + IAP headers + isolated test](0026-gar-411-telemetry-part-2.md) | [GAR-411](https://linear.app/chatgpt25/issue/GAR-411) | ✅ Merged 2026-04-21 (`412d3ee`, PR #40). CI 9/9 green.
| 0027 | [Mobile Alpha docs (OpenAPI v1 spec + QA checklist)](0027-mobile-alpha-docs.md) | GAR-332 + GAR-368 | ✅ Merged 2026-04-21 (`782bf3e`, PR #41). CI 9/9 green.
| 0028 | fix(gateway,ci): bump `rest_v1_me` retry budget 40→80 × 250ms | — (infra flake fix) | ✅ Merged 2026-04-21 (`6f457d8`, PR #42). CI 9/9 green. Follow-up do flake observado no commit `fe087e4` de plan 0027 (run 24734326510). Test local verde em 40s.
| 0029 | [GAR-358 — Mobile Settings screen (account info + logout)](0029-gar-358-mobile-settings-screen.md) | [GAR-358](https://linear.app/chatgpt25/issue/GAR-358) | ✅ Merged 2026-04-21 (`165c413`, PR #43). CI 9/9 green. Slice Flutter contido (~220 LOC): nova `SettingsScreen` `ConsumerWidget` com account card (email + UUID encurtado + data cadastro) + botão logout com dialog de confirmação + version footer; route `/settings` em `app_router.dart`; novo `Icons.settings_rounded` na AppBar do chat (popup logout existente preservado como atalho). Widget test com stub `ApiService` (2 cenários: render + logout confirm/cancel). Zero Rust, zero backend, zero dependência nova. | Docs-only slice: `docs/mobile-api-v1.yaml` (OpenAPI 3.1 formalizando os 5 endpoints `/auth/*` + `/chat/*` + `/me`, 8 schemas batendo 1:1 com os structs Rust) + `docs/mobile-qa-checklist.md` (checklist manual executável, 8 seções + release decision matrix Alpha/Beta/GA). Zero código, zero dependências. | Fecha os 4 follow-ups deferidos do plan 0025: **M3** `init()` signature break (`Result<Guard, Error>` → `Guard`) — fail-soft invariant aplicado internamente via `tracing::warn!` + empty-field guard, elimina boilerplate nos 3 callers (CLI + 2 test files); **L5** novo workflow `.github/workflows/cargo-audit.yml` (nightly cron 07:00 UTC + workflow_dispatch, `rustsec/audit-check`-compatível via `cargo install --locked cargo-audit` + `cargo audit --deny unsound --deny yanked`, off-PR path com `concurrency` guard); **SA-L-A** REDACT_HEADERS +4 cloud-LB IAP variants (`x-goog-iap-jwt-assertion`, `cf-access-jwt-assertion`, `x-ms-client-principal`, `x-forwarded-user`) + 1 novo test case; **SA-L-E** novo integration test binary `idempotent_init_isolated.rs` em process isolation — cada `.rs` em `tests/` é um binário Cargo separado, garantindo `INIT_ONCE` + Prometheus recorder globals frescos para exercitar o strong-RED deterministicamente. Testes: 15 unit + 3 + 1 + 2 = 21/21 green. Zero nova dependência Rust, +1 GitHub Action externa. |
| 0030 | [ADR batch A — 0001 inference + 0002 vector + 0004 storage + 0006 search (destravar cascatas)](0030-adr-batch-unblock.md) | GAR-371 + GAR-372 + GAR-374 + GAR-376 | ✅ Merged 2026-04-21 (`5c0449a`, PR #44). CI 9/9 green. Doc-only batch de 4 ADRs formalizando decisões já cristalizadas em `ROADMAP.md` + `deep-research-report.md`: **ADR 0001** recomenda `mistral.rs` primary + Ollama fallback (PagedAttention + Continuous Batching + KV Q8 nativos) — destrava Fase 1.1 TurboQuant+; **ADR 0002** mantém `pgvector` (benchmark B4 já cobre target com 3.6x folga) + LanceDB documentado como opção edge/offline — destrava Fase 2.1 RAG e crate `garraia-embeddings`; **ADR 0004** define trait `ObjectStore` + 3 impls (LocalFs + S3Compatible + MinIO) com versionamento obrigatório + presigned URL ≤15 min + SSE-S3 + HMAC anti-tampering + allow-list MIME + path sanitization + right-to-erasure inclui v1 — destrava GAR-394 (`garraia-storage`), GAR-395 (tus) e GAR-387 (migration 003 files); **ADR 0006** estratégia evolutiva em 3 fases (Postgres FTS S0 → Tantivy S1 → Meilisearch S2) com triggers empíricos per-grupo (p95 > 300 ms ou > 10M msg) — destrava planning Fase 3.6. Review: `@security-auditor` REQUEST CHANGES → APPROVE (2 HIGH + 3 MEDIUM + 2 LOW endereçados: allow-list MIME, HMAC integrity, erasure v1, Referer/Content-Disposition, audit presign_get_issued, path sanitization, LocalFs warn gate, TTL min 30s, SSE-KMS rotation). `@code-reviewer` REQUEST CHANGES → APPROVE (remove Rust code inline, caveat benchmark B4/B5, numera rationale 0001, unifica trigger 0006, fixa supersession filename NNNN monotônico). |
| 0031 | [Compliance docs batch — STRIDE + DPIA + Runbook ANPD/GDPR 72h](0031-compliance-docs-batch.md) | GAR-398 + GAR-399 + GAR-409 | 🟡 Em execução (sessão autônoma 2026-04-21). Batch doc-only de 3 artefatos de compliance/segurança pré-GA: **STRIDE threat model** (`docs/security/threat-model.md`) com matriz 6×STRIDE × 7 componentes (Gateway, auth, storage planned, garraia-agents LLM, plugins WASM, channels, mobile) + trust boundaries + threat inventory priorizado + controles compensatórios já shipped; **DPIA** (`docs/compliance/dpia.md`) com inventário de 13 tabelas (users, user_identities, sessions, group_members, messages+FTS, memory_items, files, audit_events, mobile_users legacy+salt, api_keys, group_invites, 8 tabelas tasks, roles/permissions) × coluna × classificação LGPD × base legal × retenção, riscos identificados + mitigações, fluxo completo de 6 direitos do titular (LGPD art. 18 / GDPR Chapter III), RoPA v1, LIA pendente para ip_inet, DPA template pendente cloud, 6 anos retention com citação Código Civil art. 205 + Lei 8.212/91, status compliance checklist; **Runbook incidentes** (`docs/compliance/incident-response.md`) com 10 seções + nova §4.5 notificação faseada (GDPR art. 33.4) + checklist §2.2 estendido (api_keys + group_invites + task_* + LLM provider breach) + matriz SLA team vs solo (solo relaxa SLAs internos mas NUNCA 72h legal) + ANPD URL caveat. Review: `@security-auditor` REQUEST CHANGES → endereçados 9 findings de completude. "Pending external legal review" (DPIA) + "pending tabletop exercise" (runbook) antes do GA. Zero código. |

## Arquivos não-versionados

Drafts ad-hoc dentro de `plans/` que **não** sigam o padrão `NNNN-*.md` ficam gitignored por design — ver `.gitignore`. Útil para rascunhos pessoais antes de formalizar um plano numerado.
