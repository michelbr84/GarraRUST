# plans/

Histórico de planos de execução do GarraIA. Cada plano está atrelado a uma issue GAR-* no Linear e é aprovado antes da execução.

## Convenção de nome

`NNNN-gar-XXX-slug-descritivo.md`

- `NNNN` — sequencial monotônico (`0001`, `0002`, ...) — ordem cronológica de criação.
- `gar-XXX` — issue Linear principal que o plano entrega.
- `slug-descritivo` — identificador humano curto em kebab-case.

## Regras

- **Aprovação obrigatória:** nenhum plano vira código sem "Plano aprovado" explícito do owner.
- **Imutável após merge:** um plano é o registro histórico de como a decisão foi tomada. Se o escopo mudar, crie um novo plano (`NNNN+1`) que o supersede.
- **Escopo claro:** `§1 Goal`, `§3 Scope/Non-scope`, `§4 Acceptance criteria` são obrigatórios.
- **Rollback plan:** todo plano precisa de `§8 Rollback plan` — se é reversível, como; se não é, por quê.
- **Open questions:** dúvidas que bloqueiam execução ficam no `§12 Open questions` e precisam ser respondidas antes do start.

## Index

| # | Plano | Issue | Status |
|---|---|---|---|
| 0001 | [OpenTelemetry + Prometheus baseline](0001-gar-384-opentelemetry-baseline.md) | [GAR-384](https://linear.app/chatgpt25/issue/GAR-384) | ✅ Merged 2026-04-13 (`84c4753`) |
| 0002 | [ADR 0003 — Database para Group Workspace](0002-gar-373-adr-postgres-decision.md) | [GAR-373](https://linear.app/chatgpt25/issue/GAR-373) | ✅ Merged 2026-04-13 (`32dba08`) |
| 0003 | [`garraia-workspace` crate + migration 001 (users & groups)](0003-gar-407-workspace-schema-bootstrap.md) | [GAR-407](https://linear.app/chatgpt25/issue/GAR-407) | ✅ Merged 2026-04-13 (`4c0f07e`) |
| 0004 | [Migration 002 — RBAC + audit_events](0004-gar-386-migration-002-rbac.md) | [GAR-386](https://linear.app/chatgpt25/issue/GAR-386) | ✅ Merged 2026-04-13 (`54cefca`, closes GAR-414) |
| 0005 | [Migration 004 — chats + messages + FTS](0005-gar-388-migration-004-chats-fts.md) | [GAR-388](https://linear.app/chatgpt25/issue/GAR-388) | ✅ Merged 2026-04-13 (`1514227`) |
| 0006 | [Migration 005 — memory_items + pgvector HNSW](0006-gar-389-migration-005-memory-pgvector.md) | [GAR-389](https://linear.app/chatgpt25/issue/GAR-389) | ✅ Merged 2026-04-13 (`d790b9a`) |
| 0007 | [Migration 007 — Row-Level Security (FORCE RLS)](0007-gar-408-migration-007-rls.md) | [GAR-408](https://linear.app/chatgpt25/issue/GAR-408) | ✅ Merged 2026-04-13 (`18d0326`) |
| 0008 | [Migration 006 — Tasks (Tier 1) com RLS embutido](0008-gar-390-migration-006-tasks-with-rls.md) | [GAR-390](https://linear.app/chatgpt25/issue/GAR-390) | ✅ Merged 2026-04-13 (`883399e`) |
| 0009 | [ADR 0005 — Identity Provider (BYPASSRLS + Argon2id + HS256)](0009-gar-375-adr-0005-identity-provider.md) | [GAR-375](https://linear.app/chatgpt25/issue/GAR-375) | ✅ Merged 2026-04-13 (`a89c783`) |
| 0010 | [`garraia-auth` crate skeleton + migration 008 (login role)](0010-gar-391a-garraia-auth-crate-skeleton.md) | [GAR-391a](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-13 (`c5d6350`) |
| 0011 | [`verify_credential` real impl + audit + JWT issuance](0011-gar-391b-verify-credential-impl.md) | [GAR-391b](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-13 (`354d72a`) |
| 0011.5 | [Migration 009 — `user_identities.hash_upgraded_at` (corretiva 391b)](0011.5-gar-391b-migration-009-hash-upgraded-at.md) | [GAR-391b](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-13 (`356e5e4`) |
| 0012 | [Axum extractor + `RequirePermission` + refresh/logout/signup endpoints](0012-gar-391c-extractor-and-wiring.md) | [GAR-391c](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-13 (`88f323e`) |
| 0013 | [RLS matrix (GAR-392) — path C, 391d deferido](0013-gar-391d-392-authz-suite.md) | [GAR-392](https://linear.app/chatgpt25/issue/GAR-392) | ✅ Merged 2026-04-14 (`4069ace` + `1267987`). GAR-391d re-escopado para plan 0014. |
| 0014 | [GAR-391d — App-layer cross-group authz matrix via HTTP](0014-gar-391d-app-layer-authz-matrix.md) | [GAR-391d](https://linear.app/chatgpt25/issue/GAR-391) | ✅ Merged 2026-04-15 (`a688497`, PR #17). 15-case table-driven matrix em `authz_http_matrix.rs` cobrindo `GET /v1/me`, `POST /v1/groups`, `GET /v1/groups/{id}` × {alice, bob, eve} + novo fixture `seed_user_without_group`. Revisões: `security-auditor` APPROVE (rule 10 satisfeita) + `code-reviewer` APPROVE. **Epic GAR-391 fechado.** |
| 0015 | [Fase 3.4 — REST `/v1` skeleton (slice 1: `GET /v1/me`)](0015-fase-3-4-rest-v1-skeleton.md) | GAR-WS-API (pré-condição GAR-391d) | ✅ Merged 2026-04-14 (`4afb4fe`, PR #8). Entregue **apenas o slice 1**: endpoint `GET /v1/me`, OpenAPI/Swagger em `/docs`, RFC 9457 Problem Details, fail-soft 503 e teste de integração fail-soft. Cobertura autenticada (`200`/`401`/`403` com JWT + Postgres real) foi deferida ao plan 0016. |
| 0016 | [Fase 3.4 — Slice 2: AppPool + harness + authed `/v1/me` + `/v1/groups` skeleton](0016-fase-3-4-slice-2-apppool-harness-groups.md) | GAR-WS-API (GAR-391d destravado após M4) | ✅ **Merged 2026-04-15** — M1 (`3d2fc66`, PR #10) `AppPool` newtype + env var + `AppState.app_pool` + split `RestV1State` + router 3-way match + stubs; M2 (`a6c337d`, PR #12) integration harness (pgvector testcontainer + typed pools + `build_router_for_test`); M3 (`b139861`, PR #13) `SecurityAddon` bearer + `seed_user_with_group` + bundled `v1_me_authed_scenarios`; M4 (`860312b`, PR #15) **real `/v1/groups` handlers** (POST create_group + GET get_group + 7-scenario integration test + `RestError::BadRequest`/`NotFound` variants + `SET LOCAL app.current_user_id` transactional protocol); M5 (`1404365`, PR #20) review follow-ups **corpo formal do plano** (Task 16 H-1/M-3/N-3: `unconfigured_handler → impl IntoResponse`, doc note PII em `RestError::Internal`, `tracing::warn!` no fallback de `problem.rs`; Task 17 M-1: `/docs/{*rest}` catch-all na branch fail-soft). **Destravou definitivamente o plan 0014 (GAR-391d).** **Itens extras do comentário de índice original** (`admin_url` accessor, `exec_with_tenant` closure API, URL parsing, `test-support` rename, `get_group` transactional wrap trigger, cenário 8 coverage gap) **não entraram neste PR por decisão de escopo** — ficam para eventual 0016.1 / 0017 se relevantes. |
| 0017 | [GAR-393 Slice 1 — `PATCH /v1/groups/{group_id}`](0017-gar-393-v1-groups-patch.md) | [GAR-393](https://linear.app/chatgpt25/issue/GAR-393) | ✅ Merged 2026-04-16 (`6077bcc`, PR #23). Slice 1 de GAR-393. Code review APPROVED + security audit 8.5/10 (no blockers). CI 9/9 green. |
| 0018 | [GAR-393 Slice 2 — `POST /v1/groups/{group_id}/invites`](0018-gar-393-v1-groups-invites.md) | [GAR-393](https://linear.app/chatgpt25/issue/GAR-393) | ✅ Merged 2026-04-16 (`7c06cec`, PR #24). Slice 2 de GAR-393. Invite creation com Argon2id token hash, migration 011 UNIQUE index, SQLSTATE 23505 conflict detection. Code review blockers addressed + security audit 8.5/10. CI 9/9 green. |
| 0019 | [GAR-393 Slice 3 — `POST /v1/invites/{token}/accept`](0019-gar-393-v1-invites-accept.md) | [GAR-393](https://linear.app/chatgpt25/issue/GAR-393) | ✅ Merged 2026-04-17 (`d590313`, PR #25). Slice 3 de GAR-393. Accept invite: Argon2id token verify, token length guard (SEC-07), expiração (410), **race-safe UPDATE guard** (`AND accepted_at IS NULL` + `rows_affected == 0` ⇒ 409), member-collision via SQLSTATE 23505 (409), INSERT `group_members` transacional. 6 tasks + review fixes. Path `/accept` em vez de `:accept` por limitação do `matchit` (Axum 0.8). Code review `REQUEST CHANGES` com 1 blocker (B-1 race) → endereçado; security audit 7.0/10 com 9 findings endereçados + 8 deferidos (SEC-01 rate-limit e SEC-04 audit_events para plan 0021). CI 9/9 green. Desbloqueia `setRole` + DELETE member (plan 0020). |
| 0020 | [GAR-393 Slice 4 — setRole + DELETE member](0020-gar-393-v1-members-setrole-delete.md) | [GAR-393](https://linear.app/chatgpt25/issue/GAR-393) | ✅ Merged 2026-04-20 (`664a276`, PR #27). Slice 4 (0020-a do split) de GAR-393 — **fecha o epic 6/6 endpoints**. `POST /v1/groups/{id}/members/{user_id}/setRole` + `DELETE /v1/groups/{id}/members/{user_id}` (soft-delete via `status='removed'`). 8 tasks + 1 review follow-ups commit. Authz em 2 camadas (capability gate com self-bypass + hierarchy gate non-self). Last-owner invariant via `COUNT(*) FILTER (WHERE role='owner' AND status='active')` pós-UPDATE na mesma tx (zero ⇒ 409 + rollback). Promote-to-owner rejeitado em `validate()` (400) + defesa DB via `group_members_single_owner_idx`. `SELECT ... FOR UPDATE` serializa setRole/DELETE concorrentes. Path `/setRole` em vez de `:setRole` por limitação Axum 0.8 / matchit (mesma decisão do plan 0019 `/accept`). Integration tests: 10 M-scenarios + 10 D-scenarios (incl. D2b admin self-leave adicionado no review follow-up) + 8 novos cases na authz matrix (26→34). Code review **APPROVED WITH NITS** (0 blockers). Security audit **8.5/10 APPROVE** (0 blockers; 2 SEC-MEDs endereçados + 1 SEC-LOW addressed). CI 9/9 green. Follow-ups para plan 0021 (0020-b): amend do predicado do partial UNIQUE index (`AND status='active'`) + audit_events + rate-limit dedicado para accept/setRole/DELETE + NITs opcionais (extrair `assert_group_header_matches` e `resolve_caller_role`). |
| 0021 | [GAR-425 — Workspace security hardening (audit + rate-limit + idx)](0021-gar-425-workspace-security-hardening.md) | [GAR-425](https://linear.app/chatgpt25/issue/GAR-425) | ✅ Merged 2026-04-20 (`4753f1d`, PR #30). Slice "0020-b" consolidando 3 follow-ups de segurança deferidos dos reviews de PRs #25 (plan 0019) e #27 (plan 0020). Entregue: (1) **Migration 012** — amendment do predicado do partial UNIQUE `group_members_single_owner_idx` para `WHERE role = 'owner' AND status = 'active'` (alinha DB × app-layer, destrava reativação futura de owners soft-deleted); (2) **Migration 013** — `WITH CHECK` explícito no `audit_events_group_or_self` RLS policy (defesa contra PERMISSIVE→RESTRICTIVE downgrade silencioso); (3) **`audit_events`** para `invite.accepted`/`member.role_changed`/`member.removed` via novo módulo `garraia-auth::audit_workspace` + helper tx-scoped, 1 INSERT por happy-path dentro da mesma tx do handler (atomicidade — rollback descarta audit + mutation juntos); (4) **Rate-limit dedicado** — preset `RateLimitConfig::members_manage()` (20/min, 200/h, burst 5) wired nas 3 rotas privilegiadas (accept, setRole, DELETE). Testes: 2 unit em WorkspaceAuditAction + audit aggregate assertion em v1_groups_scenarios + A1 audit assertion + A7 rate-limit burst. Code review **APPROVE WITH NITS** (0 blockers, 3 MEDIUMs endereçados). Security audit **8.0/10 → APPROVE** (2 HIGH blockers F-01/F-02 endereçados no commit `a3a80b9`). CI 9/9 green (2 infra-flakes do dtolnay/rust-toolchain re-disparados e passaram). Follow-ups deferidos para plan 0022 (workspace-security part 2): decodar JWT `sub` para fix do shared-bucket (F-03), `TRUSTED_PROXIES` env var (F-02 fix completo), DashMap lock refactor (code-review HIGH), rate-limiter NITs (reset_at double-call, count_in_window cast, headers duplicados), `WorkspaceAuditAction::Display` impl, assertion test de `app.current_user_id` set (F-05). |
| 0023 | [GAR-427 — Migrate `api.rs` session-IP stamp to `real_client_ip`](0023-gar-427-xff-api-session-ip.md) | [GAR-427](https://linear.app/chatgpt25/issue/GAR-427) | ✅ Merged 2026-04-20 (`cf0feea`, PR #35). Hybrid PR (plan + README + code + docblock cleanup, 4 arquivos, ~150 linhas). Narrow 1-site slice fechando 1 de 3 `TODO(plan-0023+)` markers deixados pelo plan 0022. `create_session` em `api.rs` agora deriva o IP do session token via `rate_limiter::real_client_ip` com `GARRAIA_TRUSTED_PROXIES` fail-closed (empty env ⇒ XFF ignorado). Handler signature gained `ConnectInfo(peer_addr): ConnectInfo<SocketAddr>` (direct, não `Option<>`) matching `auth_routes::login_handler` — Axum 0.8 não tem `OptionalFromRequestParts for ConnectInfo<T>`. Docblock de `real_client_ip` em `rate_limiter.rs` refreshed para refletir status de consolidação. Code review **APPROVE** (0 blockers, 1 NIT endereçado em `dd760a0`). Security audit **8/10 APPROVE** (0 blockers; 2 HIGH admin out-of-scope → plan 0024, 2 MEDIUM como follow-ups: cache TRUSTED_PROXIES parse + smoke-test ConnectInfo wiring; 2 LOW: IPv6 format docs + parse-warn counter). CI 9/9 green (Windows 8m53s). Follow-ups para plan 0024+: admin XFF consolidation (`admin/middleware.rs::extract_ip` + ~20 `admin/handlers.rs` sites requires `connect_info` propagation + product decision sobre admin audit-trail IP format). |
| 0022 | [GAR-426 — Workspace security hardening part 2 (rate-limit refinements + audit robustness)](0022-gar-426-workspace-security-part-2.md) | [GAR-426](https://linear.app/chatgpt25/issue/GAR-426) | ✅ Merged 2026-04-20 (`7e20884`, PR #33). Fechamento explícito dos 9 follow-ups deferidos dos reviews do PR #30 (plan 0021). Entregue: (1) **Per-user rate-limit key** via JWT `sub` decoding — novo `RateLimitLayerState` + `rate_limit_layer_authenticated` middleware (fix F-03: cada user ganha bucket `jwt-sub:{uuid}`); (2) **`GARRAIA_TRUSTED_PROXIES`** env + helper `real_client_ip` fail-closed (fix completo F-02: empty env ⇒ XFF ignorado); (3) **DashMap lock split** read/write em `RateLimiter::check()` + benign race documentada (CR-HIGH); (4–7) NIT sweep: `reset_at` single-call, `count_in_window` `u32::try_from`, headers deduplicados, **remoção de `X-RateLimit-Reset`** (SEC-LOW F-06 timing leak); (8) **F-05 integration test** `audit_workspace_branch_two.rs` — regression guard para branch 2 da policy `audit_events_group_or_self`; (9) **`WorkspaceAuditAction::Display`** impl (CR-NIT); (10) **A7 burst test deterministic** (20× 404 + 21º=429 exato); + review follow-ups: `#[deprecated]` em `extract_rate_limit_key`/`rate_limit_layer` legacy, 5 unit tests para fallback ladder do `extract_authenticated_rate_limit_key` (gated em `test-helpers`), `TODO(plan-0023+)` inline em `api.rs:71` + `admin/middleware.rs:168`. 9 commits T1-T8 + 1 follow-ups + 1 fix de CI (`required-features = ["test-helpers"]` gate no `[[test]]` block do novo binary). Double-verify trade-off (middleware + extractor ambos chamam `verify_access`, ~µs cost) aceito em v1. Code review **APPROVE WITH NITS** (0 blockers, 5 NITs endereçados). Security audit **8.5/10 APPROVE** (0 blockers, 2 SEC-LOWs endereçados). CI 9/9 green (Windows tomou 27m43s devido a Post Cache upload, mas sem flake). Follow-ups para plan 0023+: consolidar XFF em `api.rs`/`admin/middleware.rs` via shared `real_client_ip`, migrar `/auth/*` do deprecated middleware, cache de claims em `request.extensions()` se p95 degradar, drop das APIs deprecated quando migração completar. |
| 0024 | [GAR-412 — /metrics endpoint auth (Bearer + IP ACL + startup fail-closed)](0024-gar-412-metrics-endpoint-auth.md) | [GAR-412](https://linear.app/chatgpt25/issue/GAR-412) | ✅ Merged 2026-04-21 (`fe1f2db`, PR #38). Security hardening do subsistema de métricas Prometheus cobrindo as duas superfícies `/metrics` antes públicas: (1) listener dedicado spawnado pelo gateway em `GARRAIA_METRICS_BIND` (fail-closed **startup** quando bind não-loopback + auth ausente → `Err(MetricsExporterError::AuthNotConfigured)`, gateway principal fail-soft), (2) rota embedded `GET /metrics` no main listener (fail-closed **runtime** via middleware 401/403/503). Entrega: novo módulo `crates/garraia-gateway/src/metrics_auth.rs` com `MetricsAuthConfig` (`Arc<str>` token + `Arc<[IpNet]>` allowlist cheap-clone) + `metrics_auth_layer` Axum middleware; comparação **hash SHA-256 + `subtle::ConstantTimeEq`** (não `ct_eq` direto — elimina length-oracle, security audit M-1); `WWW-Authenticate: Bearer realm="metrics"` em toda 401 (RFC 7235 §4.1, M-2); CIDR reusa `rate_limiter::parse_trusted_proxies` (plan 0022); peer-unavailable → 503 explícito (code review MEDIUM). Novo `metrics_exporter.rs` com `spawn_dedicated_metrics_listener`. `garraia-telemetry::init_metrics` downgradeado para recorder-only (sem `with_http_listener`); `Guard::metrics_handle()` expõe clone; CLI `init_telemetry_guard → (Option<Guard>, TelemetryConfig)` flow-through uma única snapshot para o gateway (elimina dupla `from_env()`). Env vars novos: `GARRAIA_METRICS_TOKEN` (redigido em `TelemetryConfig::Debug`, listado em CLAUDE.md regra #6 ao lado de `GARRAIA_JWT_SECRET`) + `GARRAIA_METRICS_ALLOW` (comma-separated CIDR). Testes **231/231 passing** (21 unit em `metrics_auth` + 2 em `metrics_exporter` + 10 telemetry + 6 integration end-to-end com `TcpListener` real + reqwest). Code review **REQUEST CHANGES → APPROVE** (1 HIGH doc inconsistency + 2 MEDIUM + 3 LOW — todos endereçados no commit `17016aa`). Security audit **8.5/10 APPROVE** (2 MEDIUMs M-1/M-2 endereçados no mesmo commit, raising para ≥9.0). CI 9/9 green (Windows test incluso). Docs: `docs/telemetry.md` §6.1 Security (matriz 200/401/403/503 + deploy recommendations loopback/VPN/internet/escape-hatch) + §8 troubleshooting (4 novos cenários) + `.env.example` documenta as 2 vars. Breaking change para deploys com `GARRAIA_METRICS_BIND=0.0.0.0`: listener dedicado recusa bind, rota embedded 503 runtime — escape hatch `GARRAIA_METRICS_ALLOW=0.0.0.0/0` (opt-in explícito documentado). Follow-ups deferidos: plan 0025+ admin XFF consolidation (~25 sites em `admin/`, bloqueado por propagação de `ConnectInfo` + decisão de produto sobre admin audit-trail IP format), plan 0026+ `/auth/*` migration off deprecated rate-limit layer (bloqueado por design — register/login mintam o token), GAR-411 TLS em `/metrics` + cardinality guards. |
| 0025 | [GAR-411 — Telemetry hardening (REDACT_HEADERS + idempotent init + TLS docs + cardinality guards)](0025-gar-411-telemetry-hardening.md) | [GAR-411](https://linear.app/chatgpt25/issue/GAR-411) | ✅ Merged 2026-04-21 (`584c047`, PR #39). CI 9/9 green. Narrow slice fechando 4 dos 6 follow-ups de GAR-384: **M5** REDACT_HEADERS +3 entries (`x-forwarded-authorization` + `x-original-authorization` + `x-amzn-trace-id`) para reverse-proxy variants; **L3** `init()` verdadeiramente idempotente (swap `AtomicBool` → `OnceLock<()>`, segunda chamada retorna empty guard sem re-instalar tracer/recorder — regression test strong RED confirmed `attempted to set a recorder after the metrics system was already initialized` antes do fix); **M2** `docs/telemetry.md` §1.1 "TLS for OTLP exports" avisando sobre `tonic` plaintext default + `https://` endpoint recommendation + service-mesh sidecar edge-case; **M1** `debug_assert_route_template()` helper em `metrics.rs` dispara em debug build quando detecta UUID-looking segment (canonical 36-char form) ou long numeric segment (≥6 chars) no route label, `#[inline(always)]` no-op em release — zero runtime cost. **Non-scope (plan 0026+):** M3 `init()` signature change (Result→Guard, API break com 2 callers), L5 nightly cargo-audit CI workflow. Testes: 13 unit + 3 integration (idempotent_init) + 2 smoke = 18/18 green. Zero nova dependência, zero breaking API change. |
| 0026 | [GAR-411 part 2 — init signature + cargo-audit CI + IAP headers + isolated test](0026-gar-411-telemetry-part-2.md) | [GAR-411](https://linear.app/chatgpt25/issue/GAR-411) | ✅ Merged 2026-04-21 (`412d3ee`, PR #40). CI 9/9 green.
| 0027 | [Mobile Alpha docs (OpenAPI v1 spec + QA checklist)](0027-mobile-alpha-docs.md) | GAR-332 + GAR-368 | ✅ Merged 2026-04-21 (`782bf3e`, PR #41). CI 9/9 green.
| 0028 | fix(gateway,ci): bump `rest_v1_me` retry budget 40→80 × 250ms | — (infra flake fix) | ✅ Merged 2026-04-21 (`6f457d8`, PR #42). CI 9/9 green. Follow-up do flake observado no commit `fe087e4` de plan 0027 (run 24734326510). Test local verde em 40s.
| 0029 | [GAR-358 — Mobile Settings screen (account info + logout)](0029-gar-358-mobile-settings-screen.md) | [GAR-358](https://linear.app/chatgpt25/issue/GAR-358) | ✅ Merged 2026-04-21 (`165c413`, PR #43). CI 9/9 green. Slice Flutter contido (~220 LOC): nova `SettingsScreen` `ConsumerWidget` com account card (email + UUID encurtado + data cadastro) + botão logout com dialog de confirmação + version footer; route `/settings` em `app_router.dart`; novo `Icons.settings_rounded` na AppBar do chat (popup logout existente preservado como atalho). Widget test com stub `ApiService` (2 cenários: render + logout confirm/cancel). Zero Rust, zero backend, zero dependência nova. | Docs-only slice: `docs/mobile-api-v1.yaml` (OpenAPI 3.1 formalizando os 5 endpoints `/auth/*` + `/chat/*` + `/me`, 8 schemas batendo 1:1 com os structs Rust) + `docs/mobile-qa-checklist.md` (checklist manual executável, 8 seções + release decision matrix Alpha/Beta/GA). Zero código, zero dependências. | Fecha os 4 follow-ups deferidos do plan 0025: **M3** `init()` signature break (`Result<Guard, Error>` → `Guard`) — fail-soft invariant aplicado internamente via `tracing::warn!` + empty-field guard, elimina boilerplate nos 3 callers (CLI + 2 test files); **L5** novo workflow `.github/workflows/cargo-audit.yml` (nightly cron 07:00 UTC + workflow_dispatch, `rustsec/audit-check`-compatível via `cargo install --locked cargo-audit` + `cargo audit --deny unsound --deny yanked`, off-PR path com `concurrency` guard); **SA-L-A** REDACT_HEADERS +4 cloud-LB IAP variants (`x-goog-iap-jwt-assertion`, `cf-access-jwt-assertion`, `x-ms-client-principal`, `x-forwarded-user`) + 1 novo test case; **SA-L-E** novo integration test binary `idempotent_init_isolated.rs` em process isolation — cada `.rs` em `tests/` é um binário Cargo separado, garantindo `INIT_ONCE` + Prometheus recorder globals frescos para exercitar o strong-RED deterministicamente. Testes: 15 unit + 3 + 1 + 2 = 21/21 green. Zero nova dependência Rust, +1 GitHub Action externa. |
| 0030 | [ADR batch A — 0001 inference + 0002 vector + 0004 storage + 0006 search (destravar cascatas)](0030-adr-batch-unblock.md) | GAR-371 + GAR-372 + GAR-374 + GAR-376 | ✅ Merged 2026-04-21 (`5c0449a`, PR #44). CI 9/9 green. Doc-only batch de 4 ADRs formalizando decisões já cristalizadas em `ROADMAP.md` + `deep-research-report.md`: **ADR 0001** recomenda `mistral.rs` primary + Ollama fallback (PagedAttention + Continuous Batching + KV Q8 nativos) — destrava Fase 1.1 TurboQuant+; **ADR 0002** mantém `pgvector` (benchmark B4 já cobre target com 3.6x folga) + LanceDB documentado como opção edge/offline — destrava Fase 2.1 RAG e crate `garraia-embeddings`; **ADR 0004** define trait `ObjectStore` + 3 impls (LocalFs + S3Compatible + MinIO) com versionamento obrigatório + presigned URL ≤15 min + SSE-S3 + HMAC anti-tampering + allow-list MIME + path sanitization + right-to-erasure inclui v1 — destrava GAR-394 (`garraia-storage`), GAR-395 (tus) e GAR-387 (migration 003 files); **ADR 0006** estratégia evolutiva em 3 fases (Postgres FTS S0 → Tantivy S1 → Meilisearch S2) com triggers empíricos per-grupo (p95 > 300 ms ou > 10M msg) — destrava planning Fase 3.6. Review: `@security-auditor` REQUEST CHANGES → APPROVE (2 HIGH + 3 MEDIUM + 2 LOW endereçados: allow-list MIME, HMAC integrity, erasure v1, Referer/Content-Disposition, audit presign_get_issued, path sanitization, LocalFs warn gate, TTL min 30s, SSE-KMS rotation). `@code-reviewer` REQUEST CHANGES → APPROVE (remove Rust code inline, caveat benchmark B4/B5, numera rationale 0001, unifica trigger 0006, fixa supersession filename NNNN monotônico). |
| 0031 | [Compliance docs batch — STRIDE + DPIA + Runbook ANPD/GDPR 72h](0031-compliance-docs-batch.md) | GAR-398 + GAR-399 + GAR-409 | ✅ Merged 2026-04-21 (`12323c8`, PR #45). CI 9/9 green. Batch doc-only de 3 artefatos de compliance/segurança pré-GA: **STRIDE threat model** (`docs/security/threat-model.md`) com matriz 6×STRIDE × 7 componentes (Gateway, auth, storage planned, garraia-agents LLM, plugins WASM, channels, mobile) + trust boundaries + threat inventory priorizado + controles compensatórios já shipped; **DPIA** (`docs/compliance/dpia.md`) com inventário de 13 tabelas (users, user_identities, sessions, group_members, messages+FTS, memory_items, files, audit_events, mobile_users legacy+salt, api_keys, group_invites, 8 tabelas tasks, roles/permissions) × coluna × classificação LGPD × base legal × retenção, riscos identificados + mitigações, fluxo completo de 6 direitos do titular (LGPD art. 18 / GDPR Chapter III), RoPA v1, LIA pendente para ip_inet, DPA template pendente cloud, 6 anos retention com citação Código Civil art. 205 + Lei 8.212/91, status compliance checklist; **Runbook incidentes** (`docs/compliance/incident-response.md`) com 10 seções + nova §4.5 notificação faseada (GDPR art. 33.4) + checklist §2.2 estendido (api_keys + group_invites + task_* + LLM provider breach) + matriz SLA team vs solo (solo relaxa SLAs internos mas NUNCA 72h legal) + ANPD URL caveat. Review: `@security-auditor` REQUEST CHANGES → endereçados 9 findings de completude. "Pending external legal review" (DPIA) + "pending tabletop exercise" (runbook) antes do GA. Zero código. |
| 0032 | [ADR batch B — 0007 desktop frontend + 0008 doc collab (completar sweep)](0032-adr-batch-b-frontend-collab.md) | GAR-377 + GAR-378 | ✅ Merged 2026-04-21 (`74337b4`, PR #46). CI 9/9 green. Fecha os 2 últimos ADRs bloqueados do índice (8/8 populated). **ADR 0007** mantém HTML+Vanilla JS baseline como estado atual (zero complexidade premature) com trigger documentado para migrar a **SolidJS** (>10 telas, >5 componentes complexos, animação 120Hz em ≥3 superfícies, OR time ≥3 devs). SvelteKit descartado para desktop por SSR dead + file-routing desnecessário; React descartado por bundle maior. **ADR 0008** estratégia 2-fase para docs colaborativos: **S0 Tier 1** single-editor com optimistic lock `version` em Postgres (destrava Fase 3.8 Tier 1 sem CRDT ceremony); **S1 Tier 2** adota **y-crdt (yrs Rust port)** quando produto pedir collab real-time OR conflito de version > 10% das saves em 30d OR requisito "Google Docs-like". Mobile Dart caveat documentado (yrs-dart fragile → read-only mode inicial se client imaturo no trigger). Zero código. |
| 0033 | [GAR-387 — Migration 003 (folders, files, file_versions)](0033-gar-387-migration-003-files.md) | [GAR-387](https://linear.app/chatgpt25/issue/GAR-387) | ✅ Merged 2026-04-21 (`cf089a5`, PR #47). CI 9/9 green. Slot 003 reservado desde migration 004 é materializado com o schema mínimo da Fase 3.5 (ADR 0004-v1): **`folders`** (tree via parent_id), **`files`** (soft-delete + current_version pointer + size cap 5 GiB + MIME open), **`file_versions`** (immutable, (file_id,version) PK, object_key UNIQUE global, checksum_sha256 regex `^[0-9a-f]{64}$`, integrity_hmac HMAC-SHA256 hex). 3 novas tabelas entram em **FORCE RLS** (18 → 21) com policies `AS PERMISSIVE FOR ALL … USING + WITH CHECK` idênticas (pattern migration 013). Compound FKs `(folder_id, group_id) → folders(id, group_id)` e `(file_id, group_id) → files(id, group_id) ON DELETE CASCADE` fecham cross-tenant drift no DB. `garraia_app` ganha grants explícitos (003 roda antes de 007 em lex order). 9 novos cenários de teste em `migration_smoke.rs` (11-19 + inventário + 17b simetria hmac + 18b RLS WITH CHECK write-block + privilege negative matrix 24 asserções): positive, cross-group blocked, compound FK drift × 2, soft-delete não cascata, hard-delete cascata, checksum CHECK × 4, integrity_hmac CHECK × 4, object_key UNIQUE collision, RLS WITH CHECK write-block (42501), size_bytes CHECK boundaries. `@code-reviewer` APPROVE WITH NITS (I-1/I-2/I-3/S-1/S-4 endereçados). `@security-auditor` APPROVE WITH NITS 8.5/10 (SEC-M-1 deferido para GAR-394 roundtrip test; SEC-M-2/L-1 endereçados). Integration test green em 10.46s. Destrava GAR-394 (garraia-storage), GAR-395 (tus), `message_attachments` e `task_attachments`. |
| 0034 | [GAR-413 — `garraia-cli migrate workspace` spec (SQLite → Postgres)](0034-gar-413-migrate-workspace-spec.md) | [GAR-413](https://linear.app/chatgpt25/issue/GAR-413) | 🟡 Em execução (sessão autônoma 2026-04-21). **Plan-only slice** — zero código, zero schema, zero dependência. Formaliza o algoritmo normativo de `garraia migrate workspace --from-sqlite ... --to-postgres ...` consolidando ADR 0003 §Migration (data) + ADR 0005 §Migration strategy (`mobile_users → users + user_identities`). Cobre: linha de comando completa (9 flags + exit codes sysexits.h 0/1/2/64/65/67/74/75/78), 7 pré-flight checks (schema version + BYPASSRLS/superuser + anti-race re-check, confirmation gate), 10 stages ordenados por FK (users → identities com audit atômico inline → groups → chats → messages (batched + role-aware skip) → memory → sessions → api_keys → legacy audit), **PHC reassembly obrigatório** em §7.2 (mobile_users.password_hash + .salt base64 STANDARD → `$pbkdf2-sha256$i=600000,l=32$<salt_nopad>$<hash_nopad>` compatível com `password-hash::PasswordHash::new`), idempotency strategy por stage (ON CONFLICT DO NOTHING / DO UPDATE legacy_sqlite_id only / WHERE NOT EXISTS em audit), rollback com audit orphans by-design (LGPD art. 37), surface de testes (11 integration + 2 end-to-end + round-trip PHC obrigatório), 9 security review triggers. `@code-reviewer` APPROVE WITH NITS (5 items endereçados). `@security-auditor` REQUEST CHANGES → APPROVE (3 HIGH: SEC-H-1 anti-race, SEC-H-2 audit atômico, SEC-H-3 PHC reassembly — todos corrigidos). Implementação fica em follow-up slice (0034.impl — não neste PR). |
| 0035 | [GAR-379 — `garraia config check` slice 1 (validation + precedence report)](0035-gar-379-cli-config-check.md) | [GAR-379](https://linear.app/chatgpt25/issue/GAR-379) (parcial, slice 1 de N) | ✅ Merged 2026-04-21 (`095cb62`, PR #49). CI 9/9 green. Slice cirúrgico — novo subcomando `garraia config check [--json] [--strict]` expondo 3 entregáveis: (1) **precedence report** (YAML > TOML > defaults + env vars detectadas por nome + presença de `mcp.json`); (2) **validation estruturada** agregando findings em vez de falhar no primeiro erro, cobrindo 14 regras em `gateway.{port,session_ttl,session_idle,rate_limit,tls}`, `timeouts.{llm,mcp}`, `voice.{tts,stt}_endpoint`, `mcp.*.{transport,command,url}`, `fs.glob.mode`, cross-ref `memory.embedding_provider → embeddings`, cross-ref `agent.{default,fallback}_providers → llm`; (3) **exit codes sysexits** `0` / `2` (validation) / `65` (`EX_DATAERR`, arquivo não parseia). Redaction invariant: JSON nunca emite valor de `api_key`/DB URLs, só presença via boolean. `@code-reviewer` APPROVE WITH NITS (0 blockers, 3 MEDIUMs + 5 LOWs endereçados). `@security-auditor` APPROVE WITH NITS 8.6/10 (SEC-M-02 sanitise_for_display + SEC-L-01 truncate + SEC-L-02 full-redaction test endereçados; SEC-M-01 deferido para slice futuro). |
| 0036 | [GAR-382 — Argon2id replace PBKDF2 em `mobile_auth.rs` (dual-verify + lazy upgrade)](0036-gar-382-argon2id-mobile-auth.md) | [GAR-382](https://linear.app/chatgpt25/issue/GAR-382) | ✅ Merged 2026-04-21 (`bdd60ea`, PR #50). CI 9/9 green. Slice cirúrgico fechando o último caminho onde o projeto ainda escreve PBKDF2-SHA256 (600k iter) — endpoint `/auth/register` que serve o app Flutter. Reusa `garraia_auth::hashing::{hash_argon2id,verify_argon2id,consume_dummy_hash}` (RFC 9106 m=64MiB/t=3/p=4), já em prod no fluxo Postgres desde GAR-391b. Contrato: (a) novos registros → Argon2id PHC em `password_hash` + `salt=""`, (b) legacy PBKDF2 verifica via `ring::pbkdf2` + base64 salt/hash colunar, (c) após verify legacy OK, `SessionStore::update_mobile_user_hash` best-effort reescreve o row com Argon2id + salt zerado, (d) anti-enumeration via `consume_dummy_hash` no path "user not found" igualando latência a um verify real. Non-goals: mexer no schema SQLite, alterar o fluxo Postgres já Argon2id, remover PBKDF2 verify antes da migração GAR-413 impl. 4 novos integration tests `#[tokio::test]` cobrindo register→login Argon2id, legacy login→upgrade, wrong-password não upgrada, second login pós-upgrade. Rollback documentado com risco para users já upgradados. |
| 0037 | [GAR-394 — `garraia-storage` skeleton (trait ObjectStore + LocalFs)](0037-gar-394-storage-skeleton.md) | [GAR-394](https://linear.app/chatgpt25/issue/GAR-394) (parcial, slice 1 de N) | ✅ Merged 2026-04-21 (`aa48b07`, PR #51). CI 9/9 green. Slice cirúrgico — novo crate `garraia-storage` com `async_trait ObjectStore` (`put`/`get`/`head`/`delete`/`exists`/`presign_{put,get}`) + `LocalFs` baseline (path sanitization estrita + SHA-256 etag + FS roundtrip) + módulo `path_sanitize` compartilhado contra traversal (`..`, absolute paths, backslash, Windows drive letters, NUL, control chars, reserved Windows names, MAX 1024 bytes). Presigned URLs retornam `StorageError::Unsupported` em `LocalFs` — escopo para slice 2. 24 unit tests verdes. |
| 0038 | [GAR-394 slice 2 — `S3Compatible` + SSE-S3 + MIME allow-list + HMAC integrity + presigned URLs](0038-gar-394-s3-compat.md) | [GAR-394](https://linear.app/chatgpt25/issue/GAR-394) (parcial, slice 2 de N) | ✅ Merged 2026-04-21 (`34ca381`, PR #52). CI 9/9 green. Backend `S3Compatible` via `aws-sdk-s3` atrás da feature `storage-s3` (MinIO/R2/B2 cobertos via endpoint override). SSE-S3 (`ServerSideEncryption::Aes256`) hardcoded em `put`. MIME allow-list compartilhada com `LocalFs` (ADR 0004 §Security 3) + opt-out `PutOptions::allow_unsafe_mime` + HEIC/HEIF/WebM incluídos. HMAC-SHA256 integrity sobre `{object_key}:{version_id}:{sha256_hex}` com verify constant-time via `subtle::ConstantTimeEq` (ADR 0004 §Security 4). Presigned URLs reais com TTL range [30s, 900s] fail-fast (`StorageError::TtlOutOfRange`). Anti-log-injection via `StorageError::disallowed_mime` constructor. Novo trait method `get_with` com default que cai em `get` + verify. Integration tests em `tests/s3_integration.rs` com `testcontainers-modules::minio` (6 cenários, skip graceful se Docker absent). 60 unit tests verdes. Reviews: `@security-auditor` APPROVE WITH NITS 7.5/10 (2 HIGH + 6 MEDIUM/LOW endereçados); `@code-reviewer` APPROVED WITH NITS (0 blockers, 5 MEDIUM + 4 NITs endereçados). Gaps ADR 0004 rastreados p/ slice 3+ (audit_events, cross-tenant isolation, StorageConfig em garraia-config, bucket policy docs, LocalFs encryption gate, `/v1/files/*` endpoints, tus GAR-395). Non-goals: streaming `AsyncRead`/`Write`, `list`/`copy`, SSE-KMS, `MinioBackend` dedicado. |
| 0039 | [GAR-413 Stage 1 — `migrate workspace` users + user_identities + PHC reassembly](0039-gar-413-stage1-users-identities.md) | [GAR-413](https://linear.app/chatgpt25/issue/GAR-413) (parcial, stage 1/10) | ✅ Merged 2026-04-22 (`1c5cc72`, PR #53). CI 9/9 green. Implementação de §7.1 + §7.2 do plan 0034. Novo subcomando `garraia migrate workspace --from-sqlite <PATH> --to-postgres <URL> [--dry-run] [--confirm-backup]` com: (a) preflight checks — SQLite readable + `mobile_users` presente, Postgres reachable, BYPASSRLS/superuser check pre-flight + in-tx (SEC-H-1), schema version ≥013, confirmation gate quando `users.count>0` (§6.6); (b) Stage users — UPSERT com `legacy_sqlite_id`, preserva `created_at` via `$N::timestamptz`; (c) Stage identities — PHC reassembly (base64-STANDARD padded → STANDARD_NO_PAD) formato `$pbkdf2-sha256$i=600000,l=32$<salt>$<hash>` aceito por `garraia_auth::hashing::verify_pbkdf2` + audit `users.imported_from_sqlite` atômico no mesmo tx (§7.2 SEC-H-2 / LGPD art. 18); (d) idempotência via `ON CONFLICT` + `WHERE NOT EXISTS`; (e) `--dry-run` roda INSERTs dentro de tx que é rolled-back; (f) exit codes sysexits (0/64/65/67/74/78). Novo módulo `crates/garraia-cli/src/migrate_workspace.rs` (~540 LOC) + integration test `tests/migrate_workspace_integration.rs` (testcontainer pgvector+pg16 real aplicando migrations 001–013). 6 unit tests verdes incluindo round-trip `legacy_hash → phc → verify_pbkdf2(pw)==true`. |
| 0040 | [GAR-413 Stage 3 — `migrate workspace` groups + group_members](0040-gar-413-stage3-groups.md) | [GAR-413](https://linear.app/chatgpt25/issue/GAR-413) (parcial, stage 3/10) | ✅ Merged 2026-04-22 (`e9f737d`, PR #54). CI 9/9 green. Implementação de §7.4 do plan 0034 — extensão do `migrate_workspace.rs` adicionando `run_stage3_groups` dentro da mesma tx dos stages 1+2. Novos flags `--target-group-name` (default `'Legacy Personal Workspace'`) + `--target-group-type` (default `'personal'`) no subcomando. `Stage1Report` substituído por `StageReport` agregado (stages 1+2+3). Resolução de grupo via `SELECT ... FOR UPDATE` seguido de INSERT fail-closed; idempotência via `ON CONFLICT (group_id, user_id) DO NOTHING` + audit `WHERE NOT EXISTS`. Owner = primeiro user migrado por `ORDER BY created_at ASC, id ASC`. Audit atômico: `groups.imported_from_sqlite` (1×) + `group_members.imported_from_sqlite` (N×) commit com identity/membership na mesma tx (SEC-H / LGPD art. 18). Edge cases: SQLite vazio → WARN + exit 0 sem tocar tabelas; grupo preexistente (`name,type`) reaproveitado preservando owner original e evitando promoção silenciosa de user migrado. 4 novos integration tests (happy path 3 users + idempotent rerun + preexisting group reuse + empty-sqlite skip) + 2 novos unit tests (`RunOptions::default()` + `StageReport::default()`). |
| 0041 | [GAR-395 TUS 1.0 slice 1 — `POST /v1/uploads` + `HEAD /v1/uploads/{id}`](0041-gar-395-tus-slice1.md) | [GAR-395](https://linear.app/chatgpt25/issue/GAR-395) (parcial, slice 1 de 3) | ✅ Merged 2026-04-22 (`0a15d9a`, PR #55). CI 9/9 green em 3 runs. Slice 1 estabelece **contrato + modelo persistente** do servidor tus 1.0 entregando só os 2 endpoints que reservam/probam o recurso — bytes (PATCH) + commit via `ObjectStore` ficam para slice 2, expiration worker + termination (DELETE) para slice 3. **Migration 014** `tus_uploads` (FORCE RLS + policy `tus_uploads_group_isolation` com WITH CHECK explícito per migration 013 pattern, CHECK `upload_length ≤ 5 GiB` batendo com `files.size_bytes`, `object_key` UNIQUE formato ADR 0004 `{group_id}/uploads/{upload_id}/v1`, índice parcial `expires_in_progress_idx` para purge futuro). **`rest_v1::uploads`** com `create_upload` + `head_upload` handlers (Axum 0.8, `RestV1FullState`, `set_rls_context` helper para `SET LOCAL app.current_user_id` + `app.current_group_id`), `tus_version_header_layer` middleware anexando `Tus-Version: 1.0.0` em toda 412, helpers `validate_tus_resumable` + `parse_upload_length` + `parse_upload_metadata` + `reject_crlf`. `header_value_from_ascii` substitui `.expect()` calls para compliance com CLAUDE.md rule #4. Rate-limit `members_manage` preset em ambos endpoints (anti-DoS até slice 3 ter worker de expiração). Novos variants em `RestError`: `PreconditionFailed` (412) + `PayloadTooLarge` (413). `Upload-Offset` sempre `0` em slice 1. Cross-tenant → 404 (não 403, ADR 0004 §7). 14 unit tests + OpenAPI paths atualizados. Reviews: `@code-reviewer` REQUEST CHANGES → APPROVE (1 HIGH `Tus-Version` em 201, 3 MEDIUMs endereçados); `@security-auditor` REQUEST CHANGES → APPROVE 8.5/10 (2 MEDIUMs rate-limit + CRLF endereçados). |
| 0043 | [CI hygiene — strip CVSS v4 entries from advisory-db (cargo-audit scheduled fix)](0043-ci-cargo-audit-db-pin.md) | [GAR-428](https://linear.app/chatgpt25/issue/GAR-428) (sessão autônoma 2026-04-22, Lote A-0) | ✅ Merged 2026-04-22 (`39a2b96`, PR #57). Nightly `Security — cargo audit` vermelho desde 2026-04-22 08:49 UTC com `error loading advisory database: ... unsupported CVSS version: 4.0` causado por advisories CVSS v4 (`cvss = "CVSS:4.0/…"`) não suportados pelo `cvss` crate pulled transitivamente por `rustsec 0.30.x`. Upstream schema gap, não regressão. Pivotagem em 5 iterações `workflow_dispatch`: v1 pin SHA (insuficiente — deno CVSS v4 desde 2025-12-29), v2 strip regex + fetch HEAD, v3 `cargo fetch --locked` para o sparse index, v4 `.cargo/audit.toml` com 13 ignores deferidos para Lote B-2 (9 vulns + 3 unsound + 1 yanked), v5 drop `--deny yanked` (core2 unfixable hoje, dep chain image→rav1e→bitstream-io). Final: `^[[:space:]]*cvss = "CVSS:4\.0/` regex hardening (F-1 sec review) + cross-ref `.cargo/audit.toml` ↔ `deny.toml` para wasmtime overlapping (F-5). 6 deps nossos (cap-primitives, cmov, quinn-proto, tar, time, wasmtime) também perdem CVSS v4 audit coverage — todos rastreados. Reviews: `@security-auditor` 7.5/10 APPROVE WITH NITS (9 findings, 3 MEDIUM endereçados pré-merge); `@code-reviewer` APPROVE WITH NITS (3 MEDIUM + 1 NIT endereçados). Último run verde: [24795503692](https://github.com/michelbr84/GarraRUST/actions/runs/24795503692), 2m41s, 0 vulns / 0 denied / 24 allowed. **`expires_at: 2026-05-20`** — re-triage obrigatória quando rustsec 0.31+ shippar CVSS v4 support. |
| 0042 | [GAR-379 slice 2 — `[mobile]` section + `garra_persona` via config](0042-gar-379-slice2-mobile-persona.md) | [GAR-379](https://linear.app/chatgpt25/issue/GAR-379) (slice 2 de N) | 🟡 Em execução (sessão autônoma 2026-04-22). Primeiro refactor narrow do item "Remoção de leituras diretas de env/files em outros crates" de GAR-379. Nova subseção `[mobile]` em `AppConfig` com campo `persona: Option<String>`. `mobile_chat.rs::garra_persona` agora consulta `state.config.mobile.persona` primeiro, cai em `GARRA_MOBILE_PERSONA` env depois, cai em `DEFAULT_PERSONA` por último (precedência config > env > default documentada no plan §5.1). Nova regra de validação `mobile.persona < 10 chars` (WARN). 3 unit tests para a regra + 4 unit tests do 3-tier fallback via função pura `pick_persona`. Zero breaking change — operators com `GARRA_MOBILE_PERSONA` continuam funcionando. Non-goals: remover env fallback, refatorar outros env reads (JWT_SECRET, METRICS_TOKEN, OPENROUTER_API_KEY — cada um vira slice narrow próprio), expor persona via admin API, validar conteúdo além de length mínima. |
| 0044 | [GAR-395 TUS slice 2 — `PATCH /v1/uploads/{id}` + ObjectStore commit](0044-gar-395-tus-slice2.md) | [GAR-395](https://linear.app/chatgpt25/issue/GAR-395) (slice 2 de 3) | ✅ Merged 2026-04-22 (`0780d93`, PR #59). CI 9/9 green. Slice 2 entrega `PATCH /v1/uploads/{id}` (tus Core byte append) + `OPTIONS /v1/uploads` (discovery) + commit via `ObjectStore::put` + criação atômica de `files` + `file_versions` (migration 003) + audit `upload.completed` no `WorkspaceAuditAction::UploadCompleted`. Novo `StorageConfig` + `StorageBackend` enum (`local` / `s3` feature-gated) em `garraia-config::model` + 4 unit tests em `check.rs`. Staging FS local independente do backend (append-only via `tokio::fs`); commit final read-all-into-memory com cap operacional `storage.max_patch_bytes` default 100 MiB (streaming `put` → slice 3). Wiring de `ObjectStore` em `AppState` via `server.rs::build_storage_wiring`. Two-phase commit ordering (blob-first via `ObjectStore::put` + rows + audit + `UPDATE tus_uploads.status='completed'` → `COMMIT` Postgres em seguida — plan §5.3.1) — falha de `put` deixa `tus_uploads.status='in_progress'` retry-friendly. Integration test `rest_v1_uploads_patch` cobre 11 cenários (204 happy path, 204 multi-chunk 3×1 KiB, 409 offset mismatch, 415 wrong Content-Type, 412 missing Tus-Resumable, 413 body excede length, 410 completed/aborted/expired, 404 cross-group, 401 sem JWT, 502 put failure rollback, 415 disallowed MIME). Code review APROVADO COM NITS (0 blockers); security audit 8.7/10 (0 HIGH/MEDIUM, 5 LOW plan-aligned). Non-goals: DELETE termination, worker expiration, streaming put, checksum extension — slice 3. |
| 0045 | [GAR-413 Stage 5 — `migrate workspace` chats + chat_members](0045-gar-413-stage5-chats.md) | [GAR-413](https://linear.app/chatgpt25/issue/GAR-413) (parcial, stage 5/10) | ✅ Merged 2026-04-22 (`f5e61af`, PR #58). CI 9/9 green. Amendment normativo ao plan 0034 §7.5: tabela legacy real é `sessions` (não `conversations` — evidência em `garraia-db/src/session_store.rs:105`). Extensão de `migrate_workspace.rs` adicionando `run_stage5_chats` chamado após `run_stage3_groups` dentro da mesma tx. Mapping `sessions → chats` (type `'channel'`, name derivado de `metadata.title` com fallbacks para `"Chat {channel_id}"` e `"Legacy chat"`); `chat_members` com role `'owner'` para o `sessions.user_id` legacy resolvido via `users.legacy_sqlite_id`. `chats.created_by = owner_user_id` do stage 3 (survival de LGPD erasure §5.4). Audit atômico inline: `chats.imported_from_sqlite` + `chat_members.imported_from_sqlite` (`WHERE NOT EXISTS` idempotency) na mesma tx dos stages 1+2+3. `ChatMapping { session_to_chat: HashMap<String, Uuid> }` público exposto em memória para o stage 6 (messages) consumir em slice futuro. `StageReport` estendido com 6 counters novos. Edge cases cobertos: SQLite sem tabela `sessions` → WARN + skip + exit 0; sessions com `user_id` órfã (user não migrado) → skip + counter `sessions_skipped_no_user`; SQLite vazio → skip silencioso. 4 integration tests (happy path 3 sessions, idempotent rerun, no-sessions-table, orphan-user-skip) + 2 unit tests pure helpers. Non-goals: messages batched (stage 6), chat_summaries, chat_session_keys, `--only`/`--skip` flags. |
| 0046 | [GAR-379 slice 3 — JWT secret + metrics token via `[auth]` config](0046-gar-379-slice3-jwt-secret.md) | [GAR-379](https://linear.app/chatgpt25/issue/GAR-379) (slice 3 de N) | ✅ Merged 2026-04-22 (`f0ff61b`, PR #60). CI 9/9 green. Remove hardcoded fallback inseguro `"garraia-insecure-default-jwt-secret-change-me"` de `mobile_auth.rs` (grep-verified zero ocorrências em `.rs`). Novo sentinel `AuthConfigMissing` em `crates/garraia-gateway/src/state.rs` + getter `AppState::jwt_signing_secret() -> Result<SecretString, AuthConfigMissing>`; `mobile_auth.rs::issue_jwt` / `issue_jwt_pub` refactorados para propagar via `?` até handler, que converte em 503 (`JwtIssueError::AuthUnconfigured`). `GarraIA_VAULT_PASSPHRASE` consolidado em `AuthConfig::from_env` como fallback legacy via `.or_else(...)` — zero breaking change para deploys existentes. Nova `AuthSection` em `AppConfig` carrega **apenas** knobs não-secret (`jwt_algorithm = "HS256"`, `access_token_ttl_secs = 900`, `refresh_token_ttl_secs = 604800`, `metrics_token_ttl_hint_secs = 0`). `check.rs` ganha 4 validações (algoritmo aceito, TTL ranges, access ≤ refresh, env-override detection Info). Novo `docs/auth-config.md` (220 linhas) com matriz completa de precedência + troubleshooting + §4 documentação explícita do dev fail-closed 503. `metrics_token` leitura movida para módulo dedicado `garraia-telemetry::config` (precedência via `AuthConfig` na mesma sessão). 9 unit tests + 3 integration tests novos; `garraia-config` 46/46 lib tests + `garraia-gateway` 258/258 lib tests verdes. Code review APPROVE (0 blockers, 4 NITs plan-aligned); security audit 9.0/10 APPROVE (0 HIGH/MEDIUM, 3 LOW sobre topology leakage em Display de env var names — aceito). Non-goals: secrets em config file (vedado §5.1), deprecar `GarraIA_VAULT_PASSPHRASE` (slice 5), refactor de `OPENROUTER_API_KEY`/`ANTHROPIC_API_KEY`/`OPENAI_API_KEY` (slice 4), clippy lint custom (tracking §10 Q1). |
| 0047 | [GAR-395 TUS slice 3 — DELETE + expiration worker + `put_stream`](0047-gar-395-tus-slice3.md) | [GAR-395](https://linear.app/chatgpt25/issue/GAR-395) (slice 3 de 3, **epic closed**) | ✅ Merged 2026-04-23 (`96f5c03`, PR #62). CI 9/9 green (Format, Clippy strict, Test ubuntu/windows/macos, Build, E2E, Playwright, Security Audit). Fecha GAR-395 ao entregar os 3 deliverables restantes: (1) **`DELETE /v1/uploads/{id}`** (tus 1.0 Termination) idempotente — 204 quando aborta `in_progress` ou já `aborted`/`expired`, 404 cross-tenant (ADR 0004 §7), 409 se tentar abortar `completed`; (2) **Expiration worker** dedicado em novo módulo `crates/garraia-gateway/src/uploads_worker.rs` (332 LOC) + helpers puros em `uploads_worker_util.rs` (44 LOC) — loop periódico com budget configurável + jitter anti-thundering-herd, usa `expires_in_progress_idx` (migration 014) para purgar uploads `status='in_progress'` cujo `expires_at < now()`, transiciona atomic para `aborted` + emite `upload.aborted` audit. Wire em `server.rs::build_storage_wiring` atrás de feature-gate; (3) **`ObjectStore::put_stream`** em `LocalFs` — path de commit para patches grandes sem buffer integral em RAM (finalizando o cap `storage.max_patch_bytes` introduzido em slice 2 — operator pode agora elevar o limite com segurança). 752 LOC de integration tests novos em `crates/garraia-gateway/tests/rest_v1_uploads_delete_worker.rs` cobrindo termination idempotency, worker purge cycle, concurrent abort race, cross-tenant 404, put_stream success/failure rollback. Mudanças em `garraia-auth/src/audit_workspace.rs` (+19 LOC) para novo `WorkspaceAuditAction::UploadAborted`. 13 files changed, 1 712 insertions, 29 deletions. Non-goals: TUS Checksum extension (RFC extension futura), multi-region object storage, reabertura de uploads abortados. |
| 0048 | [GAR-413 Stage 6 — `migrate workspace` messages batched](0048-gar-413-stage6-messages.md) | [GAR-413](https://linear.app/chatgpt25/issue/GAR-413) (parcial, stage 6/10) | 📝 Spec commitada 2026-04-23 (`f4efa45`, Lote 0A housekeeping) — originalmente draftada em sessão autônoma paralela 2026-04-22 e deixada untracked até esta sessão. Implementação ainda não executada. Amendment normativo ao plan 0034 §7.6: SQLite legacy não tem `conversations` (tabela nunca existiu — evidência em `garraia-db/src/session_store.rs:105`); migração real é `sessions → chats → messages`. Stage 6 estende `migrate_workspace.rs` com `run_stage6_messages` chamado após `run_stage5_chats` na mesma tx. Batched reader (default 500 rows/query, herda `--batch-size` de stages anteriores), resolução `chat_id` via `ChatMapping` exposto por Stage 5 (plan 0045), resolução `sender_user_id` via `UserMapping` exposto por Stage 1 (plan 0039). UNNEST arrays em `sqlx::query!` para INSERTs de alta perf (> 10k msgs em single tx), audit agregado por batch (`WHERE NOT EXISTS` idempotency), `MessageMapping` público para Stage 7 (memory). PII redaction: `tracing::instrument(skip(body, batch))` + snapshot test `no_message_body_in_logs`. Advisory lock `pg_try_advisory_lock(hashtext('garraia_migrate_workspace'))` para impedir runs concorrentes (follow-up aberto desde plan 0039 fechado aqui). **Regra de split:** se `migrate_workspace.rs` ultrapassar 1.800 LOC **ou** Stage 6 acrescentar > 500 LOC novas, extrair para submódulo (`migrate_workspace/messages.rs`) — aprovado pelo usuário 2026-04-22. |
| 0049 | [GAR-429 Lote Q.1 — clippy strict prep + `clippy.toml` + `rustfmt.toml`](0049-gar-429-clippy-strict.md) | [GAR-429](https://linear.app/chatgpt25/issue/GAR-429) (Lote Q.1 de 3) | 🟡 Em execução (Lote B, sessão autônoma 2026-04-22). Limpeza das 26 warnings pré-existentes em `main` (too_many_arguments × 5 resolvidos via `clippy.toml too-many-arguments-threshold = 10`; collapsible_if × 7 em bootstrap.rs/chat.rs/uploads.rs/migrate_workspace.rs via let-chains Rust 2024; explicit_auto_deref × 4 em migrate_workspace.rs; assertions_on_constants × 4 nos tests router_smoke + voice_e2e removidos + sentinela comment; new_without_default × 1 com `impl Default for MockVoicePipeline`; items_after_test_module × 2 em config_cmd.rs + runtime.rs com `#[allow]` inline justificado; needless_as_bytes × 1 em server.rs; useless_conversion × 1 em rate_limiter.rs; if_same_then_else × 1 em config_cmd.rs consolidado). Flip em `.github/workflows/ci.yml` removendo `continue-on-error: true` do step clippy. Novos `clippy.toml` + `rustfmt.toml`. Marker "ClaudeMaxPower" + "Superpowers plugin" acrescentado ao topo de `CLAUDE.md`. |
| 0050 | [EPIC Quality Gates Phase 3.6 — plan foamy-origami (capability gate + 9 lotes incrementais)](0050-gar-430-quality-gates-phase-3.6.md) | [GAR-430](https://linear.app/chatgpt25/issue/GAR-430) (+ 10 sub-issues GAR-431…GAR-440) | 🟡 Em execução 2026-04-23 — **APROVADO em modo conservador** pelo usuário (Lote 0 → Lote 1 → reavaliar). Fase 0 (capability gate) + Fase 1 (diagnóstico via 3 Explore agents em paralelo) entregues com evidência objetiva: 19 crates / ~98.400 LOC Rust, `garraia-gateway` 30.191 LOC (31%), hotspots `admin/handlers.rs` 3.300 / `bootstrap.rs` 2.405 / `migrate_workspace.rs` 1.590 LOC, zero coverage tooling, zero mutation, zero fuzz, 13 RUSTSEC ignorados (expira **2026-05-20**), 3 jobs CI `continue-on-error:true` (e2e, playwright, security) mascarados como verdes, 5 crates sem integration tests (`voice`, `agents`, `channels`, `security`, `plugins`). Superpowers v5.0.7 + rust-analyzer-lsp plugins confirmados em `~/.claude/plugins/installed_plugins.json`. **awesome-claude-token-stack** MCP server validado via JSON-RPC handshake (`serverInfo.name=acts, version=0.1.0`, 16 tools expostas), DB `.acts/store.db` 176 KB + FTS5 ativo; observation `b5d7de30d2b12fee` (validação Lote 0.1) e `cfd74eb40e58c2c5` (smoke do hook stop.sh Lote 0.2) gravadas. 4 ajustes pós-aprovação aplicados: (1) Lote 0 all-or-nothing sobre MCP acts — falha vira investigação isolada; (2) Lote 3 sem serviço externo de coverage (artifact + `gh pr comment` via `GITHUB_TOKEN` default); (3) Lote 4 split 4a (rustls-webpki + idna + rsa + core2 + tokio-tar, 4 PRs pequenos) vs 4b (wasmtime 28→29 em sub-plan próprio `plans/0050.4-wasmtime-upgrade.md`); (4) Lote 6 refactor dos 3 hotspots em **18+ PRs de ≤ 500 LOC movidos** (6.1 admin/handlers.rs em 6 PRs, 6.2 bootstrap.rs em 6 PRs, 6.3 migrate_workspace.rs em 6+ PRs preservando atomicidade tx). Lote 0 completo: plan file versionado aqui, Epic + 10 sub-issues no Linear, hook `.claude/hooks/stop.sh` estendido com `acts-log.js` helper fail-soft. Próximo passo: Lote 1 (4 gates CI de baixo custo em PR único — `cargo-deny` + `dependency-review-action` + `gitleaks` + MSRV check). |

## Arquivos não-versionados

Drafts ad-hoc dentro de `plans/` que **não** sigam o padrão `NNNN-*.md` ficam gitignored por design — ver `.gitignore`. Útil para rascunhos pessoais antes de formalizar um plano numerado.
