apiVersion: v1
kind: Service
metadata:
  name: {{ include "garraia.fullname" . }}
  labels:
    {{- include "garraia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "garraia.selectorLabels" . | nindent 4 }}
